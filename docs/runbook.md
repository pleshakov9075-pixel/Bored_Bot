# Runbook

## Smoke tests

1. **Создание задач (text/image/suno).**
   - В боте: `✍️ Текст` → отправить запрос → убедиться, что задача создалась и в ответе есть `task #<id>`.
   - В боте: `🖼 Изображения` → выбрать действие/движок/режим → отправить промпт → получить `task #<id>`.
   - В боте: `🎵 Музыка` → пройти 3 шага Suno (title/tags/prompt) → получить `task #<id>`.
2. **Успешное получение результата.**
   - Дождаться сообщения `✅ Готово! (task #<id>)` с файлом/текстом.
   - Проверить в логах воркера событие `task_success` с тем же `task_id`.
3. **Сценарий таймаута.**
   - Инициировать задачу и убедиться, что бот выводит таймаут при отсутствии результата (ожидание > 30 мин).
   - Проверить, что воркер пишет событие `task_failed` с `error_message`, а `task_id` совпадает с сообщением бота.

## Feedback

* Канал сбора обратной связи — логи приложения с тегом `event=feedback`.
* Пользователь отправляет в бот: `<task_id> <комментарий>`. `task_id` совпадает с тем, что выводится в бот‑сообщениях и в воркер‑логах.
* В логах фиксируется JSON со стабильными полями `event`, `task_id`, `user_id`, `message`, что позволяет связать отзыв с `task_id` из `task_success`/`task_failed`.
